/* Curated YouTube videos, keyed "year|title|kind[|song]".
   ─────────────────────────────────────────────────────────────────
   Generated by tools/fetch-videos.mjs. Run it with a YouTube Data API
   key to resolve real video ids, so each song, making-of and interview
   shows its actual thumbnail and plays inline:

       YT_API_KEY=... node tools/fetch-videos.mjs --since 1970 --until 1979

   Until then the site links to a tightly-scoped YouTube search for each
   named song instead — which finds the right thing far more often than
   a generic query, but is still a search rather than a chosen video. */
window.VIDEOS = {};
